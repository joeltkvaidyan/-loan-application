// e-signature.cy.js
describe('E-Signature Capture (#11 P1)', () => {
  it('draws on canvas and captures signature', () => {
    cy.log('Signature canvas captures mouse/touch draw events')
  })
  it('clear removes signature and shows error on submit', () => {
    cy.log('Cleared signature prevents progression')
  })
})
